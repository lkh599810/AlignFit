fastapi==0.111.0
uvicorn[standard]==0.29.0
mediapipe==0.10.14
opencv-python-headless==4.9.0.80
numpy==1.26.4
python-multipart==0.0.9
torch==2.3.1
# mediapipe pulls in opencv-contrib-python; pin to a numpy<2 build so it stays
# compatible with torch==2.3.1 / numpy==1.26.4 (newer builds require numpy>=2).
opencv-contrib-python==4.10.0.84
