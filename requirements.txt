# AlignFit intelligent component (ML) requirements
numpy==1.26.4
pandas==2.2.2
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
optuna>=3.4
# torch pinned: the newest CPU wheel failed to load c10.dll on this Windows box
# (WinError 1114); 2.3.1+cpu is stable. Install via:
#   pip install torch==2.3.1 --index-url https://download.pytorch.org/whl/cpu
torch==2.3.1
requests>=2.28
# sqlite3 is part of the Python standard library
